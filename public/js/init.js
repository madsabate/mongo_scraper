// (function ($) {
//   $(function () {
    $(document).ready(function () {
      $('.button-collapse').sideNav();
      
      $('.parallax').parallax();
     
      // Initialize modal
      $('.modal').modal();
      // Give 'save a note' button ability to open modal
      $('.modal-trigger').modal();
    });

  // }); // end of document ready
// })(jQuery); // end of jQuery name space